{
    "scripts": {
        "lint": "web-ext lint",
        "dev": "web-ext run --firefox /var/lib/flatpak/app/app.zen_browser.zen/current/active/files/zen/zen-bin",
        "build": "web-ext build --overwrite-dest"
    },
    "devDependencies": {
        "web-ext": "^9.3.0"
    }
}